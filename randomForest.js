var records = [];

//a record contains a list of attributes
var selectRandomNodes = function(record){
	var attributeLength = record.length;
	var M = Math.sqrt(attributeLength);
}

var calculateGiniImpurity = function(){

}

var N = records.length;
